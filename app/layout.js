export const metadata = {
  title: "世界盃投注紀錄",
  description: "World Cup bet tracker",
};

export default function RootLayout({ children }) {
  return (
    <html lang="zh-Hant">
      <body>{children}</body>
    </html>
  );
}
